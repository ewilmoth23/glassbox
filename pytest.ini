[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    network: tests that require real outbound network (e.g. AISStream WebSocket live connect). Skipped by default; opt in with `pytest -m network`.
addopts = -m "not network"
