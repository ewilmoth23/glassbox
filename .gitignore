# Secrets
.env
.env.*
!.env.example
*.pem
*.key
*credentials*

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.hypothesis/
.benchmarks/
.coverage
htmlcov/
coverage.xml

# Node
node_modules/
dist/
build/

# Data & runtime
*.db
*.sqlite
*.sqlite3
*.log
logs/
data/
# `data/` is deliberately broad, but it also silently excluded two small lookup
# files the test suite requires, which cost 78 test failures until CI caught it
# on 2026-08-17. Re-include them explicitly.
!glassbox_taxonomy/data/
!glassbox_taxonomy/data/cameo_lookup.json
!ingesters/gdelt_bulk/prefilter/data/
!ingesters/gdelt_bulk/prefilter/data/source_quality.json
briefs/
_versions/
_archive/

# OS / editors
.DS_Store
.idea/
.vscode/
